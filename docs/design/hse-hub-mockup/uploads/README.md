Place the HSE logo here as:

`HSE Health & Safety Experts GmbH _Logo ohne Hintergrund 499x499px.png`

Referenced by `../HSE Hub.dc.html` (as a URL-encoded relative path). Without it, the
mockup still renders — the sidebar logo slots just show as broken images.
